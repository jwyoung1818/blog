
   <div id="main-container" class="container">
      <p align='right'>There are <%= User.first.blogs.where(category: 'admin').count %> users</p>
      <div class="row">
        <div id="sidebar" class="col-xs-2">
          <!-- sidebar content -->
            <br/>
            <br/><br/>
            <%= render partial: 'user'%>
        </div>
        <div id="content" class="col-xs-10">
            <%= render partial: 'blogs/blog'%>
            <%= render partial: 'blogs/date'%>        
        <%= link_to 'test' %>
        </div>
    </div>
